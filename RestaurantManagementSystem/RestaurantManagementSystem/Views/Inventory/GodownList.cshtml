@model IEnumerable<RestaurantManagementSystem.Models.InventoryGodown>
@{
    ViewData["Title"] = "Godown Master";
}

<h2 class="mb-4">Godown Master</h2>

<a class="btn btn-success mb-3" asp-controller="Inventory" asp-action="GodownForm">
    <i class="bi bi-plus-circle"></i> Add New
</a>

@if (TempData["ResultMessage"] != null)
{
    <div class="alert alert-info">@TempData["ResultMessage"]</div>
}

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Godown Code</th>
            <th>Godown Name</th>
            <th>Address</th>
            <th>Status</th>
            <th style="width: 280px;">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.GodownCode</td>
                <td>@item.GodownName</td>
                <td>@(string.IsNullOrWhiteSpace(item.Address) ? "-" : item.Address)</td>
                <td>
                    @if (item.IsActive)
                    {
                        <span class="badge bg-success">Active</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">Deactive</span>
                    }
                </td>
                <td>
                    <a class="btn btn-info btn-sm" asp-controller="Inventory" asp-action="GodownForm" asp-route-id="@item.Id" asp-route-isView="true">View</a>
                    <a class="btn btn-primary btn-sm" asp-controller="Inventory" asp-action="GodownForm" asp-route-id="@item.Id">Edit</a>

                    <form class="d-inline" asp-controller="Inventory" asp-action="SetGodownStatus" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@item.Id" />
                        <input type="hidden" name="isActive" value="@(!item.IsActive)" />
                        @if (item.IsActive)
                        {
                            <button type="submit" class="btn btn-warning btn-sm">Deactivate</button>
                        }
                        else
                        {
                            <button type="submit" class="btn btn-success btn-sm">Activate</button>
                        }
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
